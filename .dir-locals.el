;;; Directory Local Variables
;;; For more information see (info "(emacs) Directory Variables")

((emacs-lisp-mode . ((flycheck-emacs-lisp-load-path . inherit)
                     ;; (eval . (let
                     ;;             ((project-directory
                     ;;               (expand-file-name
                     ;;                (locate-dominating-file default-directory "org-glance.el"))))
                     ;;           (setq-local flycheck-emacs-lisp-load-path 'inherit)))
                     )))
