(require 'org-glance)

(And "^I can open \\([[:digit:]]+\\) headlines?$"
     (lambda (count-of-headlines-with-links)

       ))

(And "^I can decrypt \\([[:digit:]]+\\) headlines?$"
  (lambda (count-of-encrypted-headlines)

    ))
